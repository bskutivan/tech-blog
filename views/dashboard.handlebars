{{#if posts.length}}
<section>
  <h2>Your Posts</h2>
    {{#each posts as |post|}}
      {{> dashboard-post-info post}}
    {{/each}}

</section>
{{/if}}
<div class="flex">
    <button type="submit" class="btn" id="new-btn"><a class="link" href="/dashboard/new"> New Post</a></button>
</div>
